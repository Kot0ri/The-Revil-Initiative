##################
# SUPER EVENTS 
##################

add_namespace = super

news_event = { #Fall of Japan
	id = super.1
	hidden = yes
	is_triggered_only = yes
	timeout_days = 1
	option = {
			set_country_flag = SUPER_JAP_invasion							#country flag that handles title, should always be prefixed with SUPER_
			set_country_flag = SUPER_JAP_invasion_TEXT						#country flag that handles description should always be identical to the previous flag, but end in _TEXT
			set_variable = {BR_Super_Event_Var = token:SUPER_JAP_invasion}  #variable that stores the name of the country flag for the title so we can do some voodoo
		}
}

news_event = { #Russia CW Start
	id = super.2
	hidden = yes	
	is_triggered_only = yes
	timeout_days = 1
	option = {
		set_country_flag = SUPER_RCW_start
		set_country_flag = SUPER_RCW_start_TEXT
		set_variable = {BR_Super_Event_Var = token:SUPER_RCW_start}
		}
}

news_event = { #Russia CW End
	id = super.3
	hidden = yes	
	is_triggered_only = yes
	timeout_days = 1
	option = {
		set_country_flag = SUPER_RCW_end
		set_country_flag = SUPER_RCW_end_TEXT
		set_variable = {BR_Super_Event_Var = token:SUPER_RCW_end}
		}
}